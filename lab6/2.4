Число потомков, которые может создавать процесс не ограничено. 
#include <stdio.h>
#include <unistd.h>

int main ()
{
	int pid;
	int i;
	while(1) 
	{
		pid = fork();    
		if (pid) 
		{
			break;        
		}
    printf("Child PID %d\n", getpid()); 
    printf("My parent PID is %d\n \n", getppid());
    sleep(1);
	}
	sleep(14);
	return 0;
}
